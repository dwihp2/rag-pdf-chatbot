# 19 — Create Public Landing Page (Task 5.2)

Status: resolved
Type: task
Milestone: 5 — Root Layout & Landing Page

## Task

Create the public (unauthenticated) landing page at the root route with sign-in / create-account links.

## Files

- Modify: `src/app/page.tsx` (root) — hero + "Sign In" / "Create Account" buttons linking to `(auth)` routes

## Acceptance

- `/` shows the landing page when logged out; auth redirects still apply inside `(dashboard)`.

## Comments

- Complete; review clean. Related to the Task 4.1 fix that restored a public landing page instead of redirecting.
